{
  "name": "@mirrorai/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch --import tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@mirrorai/ai-gateway": "0.1.0",
    "@mirrorai/risk-engine": "0.1.0",
    "@mirrorai/shared": "0.1.0",
    "fastify": "^5.4.0",
    "zod": "^4.0.14"
  },
  "devDependencies": {
    "tsx": "^4.20.3"
  }
}

